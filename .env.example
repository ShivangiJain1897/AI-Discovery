# Copy to .env.local to enable live agent runs powered by Claude.
# Without a key, the platform runs in DEMO MODE using rich, deterministic
# seed data so the pilot is fully usable out of the box.
ANTHROPIC_API_KEY=

# Optional: override the model used by the agents.
# Defaults to a current Claude model.
ANTHROPIC_MODEL=claude-sonnet-5
